services:
  - type: web
    name: admin-panel
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python admin_app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0

