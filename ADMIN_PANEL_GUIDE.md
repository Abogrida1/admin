# 🔐 دليل Admin Panel المنفصل

## 📋 نظرة عامة

**Admin Panel** هو تطبيق منفصل تماماً عن التطبيق الأساسي، مخصص للمدير فقط لإدارة الاشتراكات والمستخدمين.

---

## 🎯 المميزات

### ✅ **منفصل تماماً:**
- يعمل على **port 5001** (التطبيق الأساسي على 5000)
- واجهة تسجيل دخول مستقلة
- بيانات دخول خاصة بالمدير فقط

### ✅ **متصل بنفس القاعدة:**
- يستخدم نفس `subscriptions.db`
- أي تعديل يظهر فوراً في التطبيق الأساسي
- المستخدمون يستلموا التحديثات تلقائياً

### ✅ **إدارة كاملة:**
- إدارة المستخدمين (تفعيل/تعطيل/حذف)
- إدارة الاشتراكات (إنشاء/تمديد/إلغاء)
- إدارة الخطط (إضافة/تعديل/حذف)
- مشاهدة الإحصائيات
- سجل النشاطات

---

## 🚀 التشغيل

### **الطريقة 1: ملف Batch**
```bash
RUN_ADMIN_PANEL.bat
```

### **الطريقة 2: من Terminal**
```bash
python admin_app.py
```

### **الصفحة:**
```
http://localhost:5001
```

---

## 🔑 بيانات الدخول الافتراضية

```
اسم المستخدم: admin
كلمة المرور: admin123
```

⚠️ **مهم:** غيّر كلمة المرور من ملف `admin_app.py`:
```python
ADMIN_USERNAME = "admin"
ADMIN_PASSWORD = "admin123"  # غيّر هنا
```

---

## 📊 الوظائف المتاحة

### **1. إدارة المستخدمين:**
```
- عرض جميع المستخدمين
- تفعيل/تعطيل حساب مستخدم
- حذف مستخدم
- مشاهدة تفاصيل الاشتراك
```

### **2. إدارة الاشتراكات:**
```
- إنشاء اشتراك جديد
- تمديد اشتراك موجود
- تغيير خطة المستخدم
- إلغاء اشتراك
```

### **3. إدارة الخطط:**
```
- إضافة خطة جديدة
- تعديل خطة موجودة
- حذف خطة
- تحديد السعر والمدة والحدود
```

### **4. الإحصائيات:**
```
- عدد المستخدمين
- الاشتراكات النشطة
- الاشتراكات المنتهية
- إجمالي الإيرادات
```

### **5. سجل النشاطات:**
```
- كل العمليات المسجلة
- تسجيل دخول/خروج
- تفعيل/تعطيل الحسابات
- التعديلات على الاشتراكات
```

---

## 🔄 كيف يتصل بالتطبيق الأساسي؟

### **نفس قاعدة البيانات:**
```
Admin Panel (Port 5001)
    ↓
subscriptions.db
    ↑
التطبيق الأساسي (Port 5000)
```

### **السيناريو:**
1. **المدير** يفتح Admin Panel على port 5001
2. **يعدل** اشتراك مستخدم (مثلاً يمدد 30 يوم)
3. التعديل يُحفظ في `subscriptions.db`
4. **المستخدم** يفتح التطبيق على port 5000
5. يقرأ من نفس القاعدة
6. **يشوف التحديث** فوراً!

---

## 🌐 التحديثات عبر الإنترنت

### **كيف تصل التحديثات للمستخدمين؟**

**بدون إنترنت:**
- التطبيق الأساسي يقرأ من القاعدة المحلية
- يشتغل offline تماماً

**مع الإنترنت:**
- يمكنك نشر Admin Panel على سيرفر
- المستخدمون يتحققوا من التحديثات online
- يزامنوا بياناتهم

**مثال:**
```python
# في التطبيق الأساسي (اختياري)
async def check_updates():
    try:
        response = await fetch('https://your-server.com/api/subscription-status')
        data = await response.json()
        # تحديث البيانات المحلية
    except:
        # استخدام البيانات المحلية
        pass
```

---

## 🔒 الأمان

### **✅ تم:**
- صفحة تسجيل دخول منفصلة
- Session مشفّرة
- بيانات دخول خاصة بالمدير

### **⚠️ للإنتاج:**
```python
# غيّر الـ secret key
app.secret_key = 'your-very-secret-key-here'

# استخدم HTTPS
# شغّل على port داخلي فقط (127.0.0.1)
app.run(host='127.0.0.1', port=5001)

# أو استخدم VPN للوصول عن بُعد
```

---

## 📦 الملفات

```
admin_app.py                  - التطبيق الأساسي
templates/admin_login.html    - صفحة تسجيل دخول Admin
templates/admin_panel.html    - لوحة التحكم (موجودة بالفعل)
RUN_ADMIN_PANEL.bat          - ملف تشغيل سريع
subscriptions.db             - قاعدة البيانات المشتركة
```

---

## 🎯 حالات الاستخدام

### **سيناريو 1: محلياً**
```
1. شغّل Admin Panel: RUN_ADMIN_PANEL.bat
2. افتح: http://localhost:5001
3. سجل دخول
4. عدّل الاشتراكات
5. المستخدمون يشوفوا التعديلات فوراً
```

### **سيناريو 2: عن بُعد**
```
1. نشر Admin Panel على سيرفر/VPS
2. الوصول عبر IP خاص
3. إدارة الاشتراكات من أي مكان
4. المستخدمون محلياً يزامنوا البيانات
```

### **سيناريو 3: مختلط**
```
1. Admin Panel على جهازك
2. التطبيق الأساسي عند العملاء
3. تعديلات محلية + مزامنة online
```

---

## 🔧 التخصيص

### **تغيير Port:**
```python
# في admin_app.py
app.run(debug=True, host='0.0.0.0', port=5001)  # غيّر 5001
```

### **تغيير بيانات الدخول:**
```python
# في admin_app.py
ADMIN_USERNAME = "your_username"
ADMIN_PASSWORD = "your_strong_password"
```

### **إضافة مدراء متعددين:**
```python
ADMINS = {
    'admin1': 'password1',
    'admin2': 'password2',
}

# في admin_login
if username in ADMINS and ADMINS[username] == password:
    # ...
```

---

## ⚡ نصائح

1. **شغّل Admin Panel فقط عند الحاجة** (أمان)
2. **غيّر كلمة المرور فوراً**
3. **احتفظ بنسخة احتياطية من `subscriptions.db`**
4. **لا تشارك رابط Admin Panel**
5. **استخدم VPN للوصول عن بُعد**

---

## 🆘 المشاكل الشائعة

### **Port 5001 مشغول؟**
```bash
# تحقق من العمليات
netstat -ano | findstr :5001

# غيّر Port في admin_app.py
```

### **قاعدة البيانات مقفولة؟**
```bash
# اقفل التطبيق الأساسي أولاً
# أو استخدم check_same_thread=False (موجود بالفعل)
```

### **لا يظهر التحديث؟**
```bash
# تأكد إن التطبيقين يستخدموا نفس subscriptions.db
# حدّث الصفحة في التطبيق الأساسي
```

---

## ✅ Checklist

- [ ] شغّل Admin Panel: `RUN_ADMIN_PANEL.bat`
- [ ] افتح: `http://localhost:5001`
- [ ] سجّل دخول: `admin` / `admin123`
- [ ] غيّر كلمة المرور في الكود
- [ ] جرّب إنشاء اشتراك
- [ ] افتح التطبيق الأساسي وتحقق
- [ ] خزّن بيانات الدخول بأمان

---

**🎉 Admin Panel جاهز للاستخدام!**

التطبيق الأساسي: `http://localhost:5000`  
Admin Panel: `http://localhost:5001`

